/* eslint-disable linebreak-style */
const hasCategories = (categories) => categories.lenght === 0 && categories.lenght;

// eslint-disable-next-line import/prefer-default-export
export { hasCategories };
